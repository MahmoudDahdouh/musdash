<div class="narrow">
  <h1>Welcome to musdash</h1>
  <p class="muted">
    Create the administrator account. This is the only account — musdash is
    single-user by design.
  </p>
  <% if (it.error) { %>
  <div class="notice notice-error" role="alert">
    <svg class="icon" aria-hidden="true"><use href="#i-x-circle" /></svg>
    <div><%= it.error %></div>
  </div>
  <% } %>
  <form method="post" action="/setup" class="card stack">
    <label>
      Email
      <input type="email" name="email" required autofocus autocomplete="username" />
    </label>
    <label>
      Password
      <input
        type="password"
        name="password"
        required
        minlength="12"
        autocomplete="new-password"
      />
      <small class="muted">At least 12 characters.</small>
    </label>
    <button type="submit" class="btn btn-primary">Create account</button>
  </form>
</div>
